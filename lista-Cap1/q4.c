#include <stdio.h>
#include <stdlib.h>; /* Ponto e virgula na diretiva */
int Main{} /* Trocou os parenteses por chaves*/ /*main escrito em maiusculo*/
(
printf( Existem %d semanas no ano.,52); /* Falta de aspas duplas*/
cout << endl; /* Não existe em C */
system("PAUSE");
return O; /*Foi digitado a letra o em maiusculo ao inves do número 0*/
)

/* Código certo*/

#include <stdio.h>
#include <stdlib.h>

int main (){
    printf("Existem %d semanas no ano.", 52);
    system("PAUSE");
    return 0;
}
