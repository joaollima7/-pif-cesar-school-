main() // faltou especificar o tipo main como int //
{
int a=1; b=2; c=3: // As variaves foram separadas por ponto e virgula sem declarar o tipo int, e alem disso no final da linha foi colocado dois pontos ao inves de ponto e virgula
printf("0s números são: %d%d%d\n, a, b, c, d); // Faltou fechar as aspas duplas //
system("pause");
}

// falta das bibliotecas <stdio.h> e <stdlib.h> para o printf e o system // 
// a variável 'd' não foi declarada e faltou um %d para ela no printf //
